# Permission-related keys for ~/.codex/config.toml. Merge into the existing file; do not replace it.
# Command-level rules live in default.rules.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false

# Per-tool MCP approvals, where a server's tools should run without prompting:
# [mcp_servers.chrome-devtools.tools.take_snapshot]
# approval_mode = "approve"
